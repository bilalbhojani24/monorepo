import React from 'react';

// import useReport from './useReport';

const Report = () => (
  // const { count, incrementCount, decrementCount } = useReport();

  <div className="my-4">this is report</div>
);
export default Report;

import React from 'react';
import { TMAlerts } from 'common/bifrostProxy';

const UploadFile = () => (
  <TMAlerts
    modifier="primary"
    accentBorder={false}
    title="We’re coming soon with this feature to support file based import."
    linkText={null}
  />
);

export default UploadFile;

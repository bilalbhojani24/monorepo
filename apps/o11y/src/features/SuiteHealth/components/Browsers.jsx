import React from 'react';
// import PropTypes from 'prop-types';

const Browsers = () => (
  // const { browsers } = props;
  // console.log(props);
  <div>Browsers</div>
);
Browsers.propTypes = {};

export default Browsers;

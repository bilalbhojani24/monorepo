import React from 'react';
import { twClassNames } from '@browserstack/utils';

const SnPUniqueErrors = () => (
  <div className={twClassNames('flex flex-col h-full')}>Unique Errors</div>
);

export default SnPUniqueErrors;
